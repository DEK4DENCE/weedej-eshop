import { RegisterForm } from '@/components/auth/RegisterForm'

export const metadata = {
  title: 'Create Account — Weedej',
  description: 'Join thousands of satisfied customers. Create your Weedej account today.',
}

export default function RegisterPage() {
  return <RegisterForm />
}
