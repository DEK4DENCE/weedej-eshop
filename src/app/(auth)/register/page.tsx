import { RegisterForm } from '@/components/auth/RegisterForm'

export const metadata = {
  title: 'Create Account — Weedejna',
  description: 'Join thousands of satisfied customers. Create your Weedejna account today.',
}

export default function RegisterPage() {
  return <RegisterForm />
}
